/*
import * as ACTIONTYPES from "./fixactionTypes"
import { createAction, handleActions } from 'redux-actions';
import { Map } from 'immutable';

export const create = createAction(ACTIONTYPES.CREATE);
export const modify = createAction(ACTIONTYPES.MODIFY);
export const remove = createAction(ACTIONTYPES.REMOVE); // id
export const toggleChoice = createAction(ACTIONTYPES.TOGGLE_CHOICE);
//Need a way to make it neat
//const initialState = 
*/